# Asset System v4

Every external visual is either **content**, **identity**, **explanation**, **state**, or **atmosphere**. If it fits none, it is probably decoration that can be removed.

External discovery never overrides the Design DNA. The source engine returns candidates; the Asset Director rejects most of them.
