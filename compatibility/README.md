# Compatibility Graph

The graph is advisory, not an aesthetic law. `good` means patterns commonly reinforce each other; `caution` means establish a dominant system; `avoid` means the combination regularly harms hierarchy, readability or performance unless art-directed carefully.
