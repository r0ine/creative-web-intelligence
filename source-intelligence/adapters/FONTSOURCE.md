# Fontsource Adapter

Read-only API capabilities used by v4:
- `/v1/fonts`
- `/v1/fonts/{id}`
- `/v1/variable/{id}`
- `/v1/version/{id}`

Filter candidates by required subsets, weights, style and variable capability **after** the typography brief is defined. Store only metadata needed for selection/loading.
